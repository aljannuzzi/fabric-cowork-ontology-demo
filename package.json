{
  "name": "fabric-cowork-ontology-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Ontology-grounded Fabric IQ and Cowork demo with a Data Agent exposed through MCP.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/dataAgent.js && node --check src/mcp.js && node --check src/ontology.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mssql": "^11.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
